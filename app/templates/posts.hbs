<h1> Posts </h1>

We hope you find exactly what you're looking for in a place to stay.

<ul>
  {{#each model as |post|}}
    <li>{{post.category}} - {{post.message}}
    <button {{action 'delete' post}} class="btn btn-danger btn-sm">Delete</button></li>
  {{/each}}
</ul>


<h1>New Post</h1>
  <form>
    <div class="form-group">
      <label for="category">Category</label>
      {{input value=category id="category"}}
    </div>

    <div class="form-group">
      <label for="message">Post</label>
      {{input value=message id="message"}}
    </div>
    <button class="btn btn-primary" {{action 'savePost' category message}}>Save</button>
  </form>



{{#link-to 'index'}}Home{{/link-to}}
